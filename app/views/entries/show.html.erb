<%= link_to 'Edit', edit_entry_path(@entry) %>
<h1><%= @entry.title %></h1>
<small><%=nldate @entry.created_at %></small>
<div class="Things">
  <%= @entry.things.join ' - ' %>
</div>

<p>
  <%= @entry.content %>
</p>
